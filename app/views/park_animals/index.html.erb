<h1>
  <%= @park.name %>
  Animals
</h1>

<% @park_animals.each do |animal| %>
  <%= link_to animal.animal.name, park_park_animal_path(animal.park.id, animal.id) %>
  <button>
    <%= link_to "+ Add" %>
  </button>
<% end %>

<%# @park_animals.each do |animal| %>
<!--  <div style="background-color: red; height: 50px; width: 100%;">-->
    <%#= animal.name %>
<!--  </div>-->
<%# end %>
